# =============================================================================
# CORE CONFIGURATION
# =============================================================================

# Site URL - Your application's public URL
# Development (HTTPS with local certs): https://localhost:3000
# Development (HTTP): http://localhost:3000
# Production: https://yourdomain.com
# NOTE: Must be set in both .env.local AND Convex Dashboard → Environment Variables
SITE_URL=https://localhost:3000

# Server Configuration
NODE_ENV=development
PORT=3000

# =============================================================================
# CONVEX BACKEND (Auto-generated by `bun convex dev`)
# =============================================================================

# Convex Deployment Info (CLI uses this)
CONVEX_DEPLOYMENT=dev:your-deployment-name

# Convex URLs (Frontend uses these - exposed to browser via VITE_ prefix)
VITE_CONVEX_URL=https://your-deployment.convex.cloud

# Convex Site URL (Used by Better Auth for JWKS provider configuration)
# Both are kept for backwards compatibility (code checks VITE_PUBLIC_ first, falls back to VITE_)
VITE_CONVEX_SITE_URL=https://your-deployment.convex.site
VITE_PUBLIC_CONVEX_SITE_URL=https://your-deployment.convex.site

# =============================================================================
# AUTHENTICATION & BILLING
# =============================================================================

# Autumn Billing Integration (REQUIRED)
# Get from: https://app.useautumn.com/sandbox/dev
# NOTE: Also set in Convex Dashboard → Environment Variables
AUTUMN_SECRET_KEY=your_autumn_secret_key_here

# GitHub OAuth (OPTIONAL)
# Get from: https://github.com/settings/developers
# Redirect URL: {SITE_URL}/api/auth/callback/github
# NOTE: Also set in Convex Dashboard → Environment Variables
GITHUB_CLIENT_ID=your_github_client_id_here
GITHUB_CLIENT_SECRET=your_github_client_secret_here

# =============================================================================
# ERROR TRACKING & MONITORING (Optional but Recommended)
# =============================================================================

# Sentry Error Tracking
# Get DSN from: https://sentry.io/settings/projects/YOUR_PROJECT/keys/

# Frontend Sentry (exposed to browser)
VITE_SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

# Backend Sentry (Convex functions - must also be set in Convex Dashboard)
# Can be same DSN as VITE_SENTRY_DSN or separate project
SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

# Sentry Build Tools (for source map uploads in production/CI)
SENTRY_ORG=your-org-slug
SENTRY_PROJECT=your-project-slug

# Sentry Auth Token (ONLY needed for production builds/CI source map uploads)
# Get from: https://sentry.io/settings/account/api/auth-tokens/
# Required scopes: project:read, project:releases, org:read
# SENTRY_AUTH_TOKEN=your_token_here

# =============================================================================
# ENVIRONMENT VARIABLE CHECKLIST
# =============================================================================

# Variables that need to be set in BOTH places:
# ✓ .env.local (this file)
# ✓ Convex Dashboard → Settings → Environment Variables
#
# Required in both:
# - SITE_URL
# - AUTUMN_SECRET_KEY
#
# Optional in both (if using):
# - GITHUB_CLIENT_ID
# - GITHUB_CLIENT_SECRET
# - SENTRY_DSN
#
# Frontend only (stays in .env.local):
# - VITE_CONVEX_URL
# - VITE_CONVEX_SITE_URL
# - VITE_PUBLIC_CONVEX_SITE_URL
# - VITE_SENTRY_DSN
# - CONVEX_DEPLOYMENT
# - NODE_ENV
# - PORT
#
# Build tools only (stays in .env.local):
# - SENTRY_ORG
# - SENTRY_PROJECT
# - SENTRY_AUTH_TOKEN
