# =============================================================================
# CORE CONFIGURATION
# =============================================================================

# Site URLs - Automatically selected based on NODE_ENV
# Development URL (used when NODE_ENV=development or dev)
VITE_DEV_SITE_URL=https://localhost:3000

# Production URL (used when NODE_ENV=production or prod)
# NOTE: Must also be set in Convex Dashboard → Environment Variables
VITE_SITE_URL=https://yourdomain.com

# Server Configuration
NODE_ENV=development
PORT=3000

# =============================================================================
# CONVEX BACKEND (Auto-generated by `bun convex dev`)
# =============================================================================

# Convex Cloud URL (Frontend uses this - exposed to browser)
CONVEX_URL=https://your-deployment.convex.cloud

# Convex Site URL (Used by Better Auth for JWKS provider configuration)
CONVEX_SITE_URL=https://your-deployment.convex.site

# =============================================================================
# AUTHENTICATION & BILLING (Private - NOT exposed to browser)
# =============================================================================

# Better Auth Secret (REQUIRED)
# Generate: openssl rand -base64 32
BETTER_AUTH_SECRET=your_better_auth_secret_here

# Autumn Billing Integration (REQUIRED)
# Get from: https://app.useautumn.com/sandbox/dev
# NOTE: Also set in Convex Dashboard → Environment Variables
AUTUMN_SECRET_KEY=your_autumn_secret_key_here

# GitHub OAuth - Development (OPTIONAL)
# Get from: https://github.com/settings/developers
# Redirect URL: {VITE_DEV_SITE_URL}/api/auth/callback/github
# Used when NODE_ENV=development or dev
DEV_GITHUB_CLIENT_ID=your_dev_github_client_id_here
DEV_GITHUB_CLIENT_SECRET=your_dev_github_client_secret_here

# GitHub OAuth - Production (OPTIONAL)
# Get from: https://github.com/settings/developers
# Redirect URL: {VITE_SITE_URL}/api/auth/callback/github
# Used when NODE_ENV=production or prod
# NOTE: Also set in Convex Dashboard → Environment Variables
GITHUB_CLIENT_ID=your_prod_github_client_id_here
GITHUB_CLIENT_SECRET=your_prod_github_client_secret_here

# =============================================================================
# ERROR TRACKING & MONITORING (Optional but Recommended)
# =============================================================================

# Sentry Error Tracking (exposed to browser)
# Get DSN from: https://sentry.io/settings/projects/YOUR_PROJECT/keys/
SENTRY_DSN=https://examplePublicKey@o0.ingest.sentry.io/0

# Sentry Organization & Project (exposed to browser)
SENTRY_ORG=your-org-slug
SENTRY_PROJECT=your-project-slug

# Sentry Auth Token (Private - ONLY for production builds/CI source map uploads)
# Get from: https://sentry.io/settings/account/api/auth-tokens/
# Required scopes: project:read, project:releases, org:read
SENTRY_AUTH_TOKEN=your_token_here

# =============================================================================
# DEVELOPER REPORTS (Optional)
# =============================================================================

# CodeRabbit API Integration (Private)
# Get from: https://coderabbit.ai/dashboard/api-keys
# NOTE: Also set in Convex Dashboard → Environment Variables
CODERABBIT_API_KEY=your_coderabbit_api_key_here

# =============================================================================
# ENVIRONMENT VARIABLE CHECKLIST
# =============================================================================

# Variables that need to be set in BOTH places:
# ✓ .env.local (this file)
# ✓ Convex Dashboard → Settings → Environment Variables
#
# Required in both:
# - VITE_DEV_SITE_URL (development)
# - VITE_SITE_URL (production)
# - AUTUMN_SECRET_KEY
# - BETTER_AUTH_SECRET
#
# Optional in both (if using):
# - DEV_GITHUB_CLIENT_ID + DEV_GITHUB_CLIENT_SECRET (development)
# - GITHUB_CLIENT_ID + GITHUB_CLIENT_SECRET (production)
# - SENTRY_DSN
# - CODERABBIT_API_KEY
#
# Frontend only (stays in .env.local):
# - CONVEX_URL
# - CONVEX_SITE_URL
# - SENTRY_DSN
# - SENTRY_ORG
# - SENTRY_PROJECT
# - NODE_ENV
# - PORT
#
# Build tools only (stays in .env.local):
# - SENTRY_AUTH_TOKEN
