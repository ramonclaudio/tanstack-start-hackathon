[build]
  command = "vite build"
  publish = "dist/client"

[build.environment]
  # Whitelist public env vars that are intentionally exposed to client
  SECRETS_SCAN_OMIT_KEYS = "VITE_DEV_SITE_URL,VITE_SITE_URL,CONVEX_URL,CONVEX_SITE_URL,SENTRY_DSN,SENTRY_ORG,SENTRY_PROJECT"
